export const FRAGMENT_PROTOTYPE_CONFIG = Object.freeze({
  // Réglages provisoires du PACK 4 — non canoniques et non finaux.
  activeCount: 6,
  visualRadiusPixels: 7,
  absorptionRadiusPixels: 25,
  spawnMarginPixels: 48,
  minimumHeadDistancePixels: 90,
  minimumBodyDistancePixels: 28,
  minimumFragmentDistancePixels: 38,
  maximumSpawnAttempts: 24
});
